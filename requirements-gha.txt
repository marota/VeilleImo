requests>=2.31
beautifulsoup4>=4.12
PyYAML>=6.0
# Playwright n'est nécessaire QUE pour le collecteur de secours (headless).
# Le collecteur principal (ScrapingBee) n'utilise que requests + bs4.
# playwright>=1.44
